{
  "schemaVersion": 2,
  "id": "omamail",
  "name": "Omamail",
  "version": "0.1.1",
  "author": "Jason Lee <huacnlee@gmail.com>",
  "license": "MIT",
  "description": "Read, triage, and reply to Gmail in a native nbshell window using the official Gmail API.",
  "kinds": [
    "service",
    "bar-widget",
    "panel"
  ],
  "activation": "on-demand",
  "entryPoints": {
    "service": "Service.qml",
    "barWidget": "BarWidget.qml",
    "panel": "App.qml"
  },
  "barWidget": {
    "displayName": "Omamail",
    "description": "Unread count in the bar; the mailbox itself opens as its own window.",
    "category": "Communication",
    "aliases": [
      "gmail",
      "mail",
      "email",
      "inbox"
    ],
    "allowMultiple": false,
    "defaultSection": "right",
    "defaults": {
      "refreshIntervalSec": 120,
      "maxMessages": 25,
      "defaultQuery": "in:inbox",
      "notifyNewMail": "On",
      "oauthPort": 9481,
      "openOnClick": "Window"
    },
    "schema": [
      {
        "key": "refreshIntervalSec",
        "type": "integer",
        "label": "Check for mail every (seconds)",
        "min": 30,
        "max": 3600,
        "step": 30,
        "defaultValue": 120,
        "description": "How often the unread count is refreshed while the panel is closed."
      },
      {
        "key": "maxMessages",
        "type": "integer",
        "label": "Messages per page",
        "min": 5,
        "max": 100,
        "step": 5,
        "defaultValue": 25
      },
      {
        "key": "defaultQuery",
        "type": "string",
        "label": "Default search",
        "defaultValue": "in:inbox",
        "description": "Any Gmail search operator, for example `in:inbox -category:promotions`."
      },
      {
        "key": "notifyNewMail",
        "type": "enum",
        "label": "Notify on new mail",
        "options": [
          "On",
          "Off"
        ],
        "defaultValue": "On",
        "description": "Sends a desktop notification when a new unread message arrives in the inbox."
      },
      {
        "key": "oauthPort",
        "type": "integer",
        "label": "Sign-in callback port",
        "min": 1024,
        "max": 65535,
        "step": 1,
        "defaultValue": 9481,
        "description": "Loopback port used once during Google sign-in. Change it only if something else already listens there."
      },
      {
        "key": "openOnClick",
        "type": "enum",
        "label": "Clicking the bar icon opens",
        "options": [
          "Window",
          "Quick preview"
        ],
        "defaultValue": "Window",
        "description": "The full window, or a small card with the most recent unread mail."
      }
    ]
  }
}

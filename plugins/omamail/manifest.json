{
  "schemaVersion": 2,
  "id": "omamail",
  "name": "Mail",
  "version": "0.2.0",
  "author": "Jason Lee <huacnlee@gmail.com>",
  "license": "MIT",
  "repository": "https://github.com/huacnlee/omamail",
  "dependencies": {
    "commands": ["curl", "secret-tool", "socat", "openssl", "xdg-open"],
    "packages": ["curl", "libsecret", "socat", "openssl", "xdg-utils"]
  },
  "description": "Read, triage, and reply to Gmail and IMAP mail in a native nbshell window.",
  "kinds": [
    "service",
    "bar-widget",
    "panel"
  ],
  "activation": "on-demand",
  "entryPoints": {
    "service": "Service.qml",
    "barWidget": "BarWidget.qml",
    "panel": "App.qml"
  },
  "barWidget": {
    "displayName": "Mail",
    "description": "Unread mail count in the bar; Gmail and IMAP mailboxes open in a native window.",
    "category": "Communication",
      "aliases": [
      "mail",
      "email",
      "gmail",
      "imap",
      "inbox"
    ],
    "allowMultiple": false,
    "defaultSection": "right",
    "defaults": {
      "refreshIntervalSec": 120,
      "maxMessages": 25,
      "defaultQuery": "in:inbox",
      "notifyNewMail": "On",
      "oauthPort": 9481,
      "openOnClick": "Window"
    },
    "schema": [
      {
        "key": "refreshIntervalSec",
        "type": "integer",
        "label": "Check for mail every (seconds)",
        "min": 30,
        "max": 3600,
        "step": 30,
        "defaultValue": 120,
        "description": "How often the unread count is refreshed while the panel is closed."
      },
      {
        "key": "maxMessages",
        "type": "integer",
        "label": "Messages per page",
        "min": 5,
        "max": 100,
        "step": 5,
        "defaultValue": 25
      },
      {
        "key": "defaultQuery",
        "type": "string",
        "label": "Default search",
        "defaultValue": "in:inbox",
        "description": "Gmail accounts accept Gmail search operators; IMAP accounts accept IMAP SEARCH criteria."
      },
      {
        "key": "notifyNewMail",
        "type": "enum",
        "label": "Notify on new mail",
        "options": [
          "On",
          "Off"
        ],
        "defaultValue": "On",
        "description": "Sends a desktop notification when a new unread message arrives in the inbox."
      },
      {
        "key": "oauthPort",
        "type": "integer",
        "label": "Sign-in callback port",
        "min": 1024,
        "max": 65535,
        "step": 1,
        "defaultValue": 9481,
        "description": "Loopback port used once during Google sign-in. IMAP accounts do not use it."
      },
      {
        "key": "openOnClick",
        "type": "enum",
        "label": "Clicking the bar icon opens",
        "options": [
          "Window",
          "Quick preview"
        ],
        "defaultValue": "Window",
        "description": "The full window, or a small card with the most recent unread mail."
      }
    ]
  }
}
